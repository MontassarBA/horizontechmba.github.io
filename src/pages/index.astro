---
import Layout from '../layouts/Layout.astro';

const LANGUAGES = [
	{
		code: 'fr',
		label: 'Français',
		flag: '🇫🇷',
		path: '/fr/'
	},
	{
		code: 'en',
		label: 'English',
		flag: '🇬🇧',
		path: '/en/'
	}
];
---

<Layout title="Bienvenue | Welcome">
	<section class="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-primary-50 via-white to-secondary-50">
		<div class="max-w-xl w-full mx-auto p-8 bg-white/80 rounded-3xl shadow-2xl shadow-black/10 border border-gray-100 text-center">
			<h1 class="text-3xl md:text-4xl font-bold mb-6 text-gray-900">
				Bienvenue chez HORIZONTECH MBA<br/>
				<span class="text-primary-600">Welcome</span>
			</h1>
			<p class="mb-8 text-gray-600 text-lg">
				Choisissez votre langue / Please select your language
			</p>
			<div class="flex flex-col sm:flex-row gap-6 justify-center">
				{LANGUAGES.map(lang => (
					<a
						href={lang.path}
						class="flex-1 px-8 py-5 rounded-xl border-2 border-primary-200 bg-primary-50 hover:bg-primary-100 hover:border-primary-400 text-xl font-semibold flex flex-col items-center gap-2 transition-all shadow-md hover:shadow-lg"
						style="min-width:160px"
					>
						<span class="text-3xl">{lang.flag}</span>
						{lang.label}
					</a>
				))}
			</div>
		</div>
	</section>
</Layout>
